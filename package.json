{
  "name": "tsokoban",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "build:single": "tsc && MULTIPLAYER=0 vite build",
    "preview": "vite preview",
    "server": "bun server/room.ts"
  },
  "devDependencies": {
    "typescript": "~5.9.3",
    "vite": "^7.2.4"
  }
}
